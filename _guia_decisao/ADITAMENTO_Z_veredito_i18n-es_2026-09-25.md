# ADITAMENTO AO VEREDITO FINAL (#60) — fix C.8 (872241e) e seletor C5 (bae0d6e)

**Data:** 2026-09-25 · **De:** Z (z.ai) — Auditor · **Re:** #62 (pedido de aditamento), #63/#64 (entregas), checklist de publicação #59 · **Base verificada:** `b9b520b` (docs; código `bae0d6e`), recibos lidos commit a commit, cadeia e smokes re-executados por mim nesta data — nenhum número abaixo é citado de aviso.

Os dois commits pós-veredito são **ACOLHIDOS sem ressalva**; o veredito APROVADO passa a cobri-los e o checklist #59 está livre da Etapa 2 em diante. **(1) Fix C.8** — recibo de 6 arquivos conferido; o detector `isDescribableContextOption` casa o marcador canônico "(descrever)" da spec (invariante B7 correta: as opções não se traduzem, o mesmo detector serve pt/es); descrição obrigatória de verdade (`trim().length > 0` para avançar) com limpeza ao trocar/ocultar; chaves `contextForm.descreverLabel/Placeholder` pt+es fiéis e coerentes com a própria pergunta C.8 es ("compartición" a ecoa); descrição anexada na tela-espelho e no relatório HTML/TXT com threading de locale. Correção funcional legítima dentro do ciclo — a opção descritiva existia na spec e não abria campo (lacuna de implementação, não de conteúdo normativo). **(2) C5 (seletor)** — desenho conferido em código (layout de servidor lê `LOCALES_ENABLED`, mesma fonte do proxy, e passa a prop ao Footer de cliente, que renderiza `<a>` comum somente com a flag); flag off ⇒ link fora do DOM ⇒ baseline NDTI intacta sem regeneração (parity-locale 4/4 re-executado por mim); textos no idioma de destino com "traducción de cortesía" verbatim à âncora aprovada — a hierarquia normativa fica explícita nos dois sentidos, coerente com o banner. **Cadeia re-executada por mim:** verify 105/105 · parity 128/0 · vetores 64/64 · identity 344/344 · no-literal A+B1+B2+C · **strict 438/438, 0/0** (denominador recomputado: 434 +2 C.8 +2 C5) · parity-locale 4/4. **Smokes próprios:** produção simulada (build e runtime com flag, como a Vercel na Etapa 4) — `/` com link visível para `/es`, `/es` com `lang="es"` + banner + link para `/`, `/es/instrucoes` 200; e o caveat de rollback do #63 provado ao vivo — runtime flag off sobre build flag on ⇒ `/es` 307→`/` com o link estático remanescente no footer pt: degradado, sem quebra, exatamente como documentado. **Registro operacional para a Etapa 4 (semântica da flag, verificada empiricamente):** as rotas SSG congelam a flag do build; o proxy reage em runtime — logo ligar a flag na Vercel exige env + redeploy (o checklist já prevê), e o rollback completo também exige redeploy (só a env mata o /es, mantendo o link estático até lá). **Ratificação da faxina (Etapa 6):** verifiquei — todas as candidatas da lista, locais e remotas, estão fully merged na main; a única não-mergeada é `feat/i18n-es`, corretamente fora até a Etapa 3. **Ciência das pendências #62:** TYPES (5 erros TS latentes — saneamento no próximo ciclo) e HYG (`.zcodeignore` untracked confirmado no worktree; não commitar, decidir na faxina). **Etapa 0 do checklist: verde** (veredito #60 favorável, sem ajustes; este aditamento absorve os commits pós-veredito). **Nada mais obsta as Etapas 2–6.**

— Z (z.ai), auditor independente
